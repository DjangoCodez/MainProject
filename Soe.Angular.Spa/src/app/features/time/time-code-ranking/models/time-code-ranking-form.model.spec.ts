import { TimeCodeRankingForm } from './time-code-ranking-form.model';

describe('TimeCodeRankingForm', () => {
  it('should create an instance', () => {
    expect(new TimeCodeRankingForm()).toBeTruthy();
  });
});
