import { MinutesToTimeSpanPipe } from './minutes-to-time-span.pipe';

describe('MinutesToTimeSpanPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesToTimeSpanPipe();
    expect(pipe).toBeTruthy();
  });
});
