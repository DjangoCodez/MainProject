export interface ISoeCountInfoOptions {
  hidden?: boolean;
  pinnedLeftText?: string;
  prefixText?: string;
  termTotal?: string;
  termFiltered?: string;
  termSelected?: string;
  tooltip?: string;
}
