using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Soe.Business.Test.CompDBCache
{
    [TestClass]
    public class UserCompanySettingsTest
    {
        [TestMethod]
        public void GetCompanySettings()
        {
           // SettingManager sm = new SettingManager(null);
           //var company = sm.GetCompanySettings(7);
           // var user = sm.GetUserSettings(83);
           // var app = sm.GetApplicationSettings();

            Assert.IsTrue(true);
        }
    }
}
