using System;

namespace SoftOne.Soe.Web.modalforms
{
    public partial class RegNote : PageBase
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            //if (F.Count > 0)
            //{
                //GenericHandler gh = new GenericHandler(ParameterObject);
                
                //AnteckningEntity note = new AnteckningEntity();
                //GenericHandler.PopulateEntity(note, F);
                //note.SkapadAvAnvId = ParameterObject.AnvId;
                //note.AktorForetagId = 3;
                //gh.SaveEntity(note);
                
                //Response.Redirect(Request.UrlReferrer.ToString());
            //}
        }
    }
}